# FIRELINE_V2

The original uploaded HTML is preserved as `index.html` without modifying its contents.

## GitHub

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin <YOUR_GITHUB_REPO_URL>
git push -u origin main
```

## Vercel

Import this GitHub repository into Vercel. The project is a static HTML site and `index.html` is the entry point.

> Note: the HTML is intentionally unchanged, so any behavior already present in the original file is also unchanged.
